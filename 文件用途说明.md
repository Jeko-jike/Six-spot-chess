# 文件夹内容说明

## 游戏运行必需

- `index.html`：GitHub Pages 和普通网站使用的网页入口，必须保留并上传。
- `six_go.html`：本机直接打开的同款游戏文件，也是训练导入器更新的源文件，建议保留。
- `server.js`：本机联网房间后端；需要通过本机开房联机时必须保留。
- `启动游戏.bat`：启动 `server.js` 和 Cloudflare 临时公网通道。
- `cloudflared-windows-amd64.exe`：Cloudflare Tunnel 程序；使用本机公网联机时需要，单机游戏或仅训练时不需要。

## 对抗训练必需

- `一键对抗训练.bat`：一键训练两种棋并自动导入网页。
- `adversarial-train.js`：对抗式训练主程序。
- `import-ai-weights.js`：把训练冠军写入两个 HTML 文件。
- `adversarial-state-13x13-win5.json`：五子棋冠军、历史轮次和竞技场记录。
- `adversarial-state-19x19-win6.json`：六子棋冠军、历史轮次和竞技场记录。
- `adversarial-replay-13x13-win5.json.gz`：五子棋压缩经验回放池。
- `adversarial-replay-19x19-win6.json.gz`：六子棋压缩经验回放池。
- `trained-weights-13x13-win5.json`：当前五子棋冠军导出权重。
- `trained-weights-19x19-win6.json`：当前六子棋冠军导出权重。

状态文件与回放池建议备份。删除它们不会破坏网页中已经嵌入的 AI，但下次对抗训练会失去历史进度或经验数据。

## 旧版训练器文件

- `train-ai.js`：旧的普通自对弈训练器。对抗训练已经取代它，但保留可用于对照或继续旧训练。
- `ai-checkpoint-13x13-win5.json`：旧训练器的五子棋检查点。
- `ai-checkpoint-19x19-win6.json`：旧训练器的六子棋检查点。
- `AI离线训练说明.md`：旧训练器说明。

如果以后只使用 `一键对抗训练.bat`，以上四项可以移到备份目录；它们不是游戏或新对抗训练的必需文件。

## 按部署方案选择保留

- `cloudfunctions/room/`：腾讯 CloudBase 云函数版本的房间后端。只有部署 CloudBase 时需要。
- `CLOUDBASE部署说明.md`：CloudBase 部署说明。
- `本机联网使用说明.md`：本机 Node.js + Cloudflare 联机说明。
- `对抗训练说明.md`：新对抗训练说明，建议保留。

## 可直接删除

- `提示词.txt`：当前为空，对游戏和训练没有作用。

## 最小组合示例

只在 GitHub Pages 发布单机游戏：只需上传 `index.html`。

本机运行并通过 Cloudflare 联机：保留 `index.html`、`six_go.html`、`server.js`、`启动游戏.bat` 和 `cloudflared-windows-amd64.exe`。

继续对抗训练：额外保留一键 BAT、两个训练 JavaScript、两套 `adversarial-state`、两套 `adversarial-replay` 和两套 `trained-weights` 文件。
