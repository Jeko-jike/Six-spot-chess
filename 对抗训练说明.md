# 一键对抗训练说明

双击 `一键对抗训练.bat` 即可依次训练两种棋，并自动把当前冠军模型导入 `six_go.html` 与 `index.html`。

训练流程为：

1. 当前冠军与挑战者交替执黑、执白自对弈。
2. 将局面特征、候选落点和最终胜负写入压缩回放池。
3. 用回放池训练挑战者的策略头和价值头。
4. 挑战者与冠军进行独立竞技场测试。
5. 挑战者得分达到门槛才晋级，否则保留旧冠军。
6. 五子棋和六子棋都完成后自动嵌入网页。

## 时间与空间

默认每次双击会在已有进度上继续增加 4 轮，而不是清空重练。根据电脑性能，一次完整运行可能需要数小时；六子棋通常更慢。

训练状态和回放池分别保存为：

- `adversarial-state-13x13-win5.json`
- `adversarial-replay-13x13-win5.json.gz`
- `adversarial-state-19x19-win6.json`
- `adversarial-replay-19x19-win6.json.gz`

默认每种棋最多保留 250,000 个训练样本。压缩回放池可能逐渐增加到几十或几百 MB，但只有轻量冠军权重会导入 HTML。

关闭窗口时，已经完成的整轮会保留；正在进行的本轮需要下次重新计算。

## 调整训练强度

右键编辑 `一键对抗训练.bat`，可修改开头的配置：

- `ROUNDS`：本次新增训练轮数。
- `GOMOKU_SELF_GAMES`：每轮五子棋自对弈局数。
- `CONNECT6_SELF_GAMES`：每轮六子棋自对弈局数。
- `ARENA_GAMES`：每轮新旧模型考试局数。
- `REPLAY_POSITIONS`：每种棋最多保留的训练样本数。
- `EPOCHS`：每轮回放池训练次数。
- `HIDDEN_UNITS`：轻量网络隐藏单元数。
- `PROMOTE_SCORE`：挑战者晋级所需得分率，默认 `0.53`。

更改 `HIDDEN_UNITS` 时，旧冠军会自动扩展或裁剪到新尺寸。一般建议保持 32；提高到 48 或 64 会增加训练及网页推理时间。

## 发布

训练完成后，本机文件已经自动更新。若使用 GitHub Pages，还需要把最新的 `index.html` 提交并推送到 GitHub。
