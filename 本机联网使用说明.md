# 本机联网对战使用说明

## 第一次准备

1. 安装 Node.js LTS：https://nodejs.org/
2. 下载 `cloudflared.exe`：https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/downloads/
3. 将 `cloudflared.exe` 放到本游戏文件夹中。

## 每次开服

1. 双击 `启动游戏.bat`，不要关闭弹出的服务器窗口。
2. 在游戏中选择五子棋或六子棋，再选择“线上对战”并创建房间。
3. 在游戏文件夹空白处按住 Shift 点击鼠标右键，选择“在终端中打开”，运行：

   ```powershell
   .\cloudflared.exe tunnel --url http://localhost:8765
   ```

4. 等待终端显示 `https://随机名称.trycloudflare.com`，用手机打开这个地址。
5. 房主也建议通过这个公网地址重新进入，然后创建房间并复制邀请给朋友。

## 注意事项

- 双方不需要处于同一个网络。
- 服务器窗口和 cloudflared 窗口必须一直保持开启，电脑不能休眠。
- 临时网址每次启动通常不同，房间数据在服务器重启后清空。
- 房间最多两名玩家，超过 24 小时未活动的房间会自动清理。
- 若朋友打不开链接，可稍后重新启动 Tunnel；中国大陆不同网络下的连接质量可能不同。

## 提高联网质量

- 开服电脑尽量使用网线或稳定的 5 GHz Wi-Fi，并关闭系统自动休眠。
- 关闭占用上行带宽的网盘同步、直播、下载和视频上传。
- 两位玩家都使用同一个 `trycloudflare.com` 邀请地址进入，不要一人使用 `localhost`、另一人使用公网地址。
- 如果明显卡顿，按 `Ctrl+C` 关闭 Tunnel 后重新运行，获得新的临时线路和网址。
- 临时 Tunnel 的路由质量由网络运营商和 Cloudflare 决定，程序无法保证固定延迟。若要长期稳定，需要监护人管理的正式服务器或国内合规托管。

联机页面采用即时本地落子、约 0.4 秒同步和在线心跳。对方约 6.5 秒没有心跳时显示掉线，恢复访问后自动显示重新连接。AI 支招、悔棋和重新开始均需对手确认，申请 30 秒后失效。
